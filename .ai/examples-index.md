# Examples Index

Working examples:

- `endpoints/examples/url-inspector/`: x402 endpoint demo.
- `endpoints/examples/url-inspector-with-memory/`: memory-backed URL inspector.
- `memory/examples/reflect-session.ts`: reflection pipeline demo.
- `tools/bankr/examples/dry-run-swap.ts`: Bankr dry-run receipt demo.

Optional scaffold templates:

- `research-agent`: research workflow.
- `x402-paid-agent`: paid endpoint workflow.
- `memory-agent`: memory-first workflow.
- `planner-agent`: planner/executor workflow.
- `trading-agent`: dry-run economic workflow.
- `watchdog-agent`: checkpointed monitor workflow.

Templates are executable structure examples. Use them when they speed up implementation, but natural-language task routing and primitive contracts remain the main workflow.

When adding an example, include README, env docs, tests, evals, observability, and reproduction commands.
